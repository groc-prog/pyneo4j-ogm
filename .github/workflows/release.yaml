name: Release

on:
  workflow_run:
    workflows: [Quality]
    # branches:
    #   - main
    types:
      - completed

jobs:
  release:
    runs-on: ubuntu-latest
    needs: [quality, tests]
    steps:
      - name: foo
        run: echo "foo"
