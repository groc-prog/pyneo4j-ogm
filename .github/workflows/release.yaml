name: Package release

on:
  workflow_run:
    workflows: [Quality checks and tests]
    types:
      - completed

jobs:
  release:
    runs-on: ubuntu-latest
    needs: [quality, tests]
    steps:
      - name: foo
        run: echo "foo"
